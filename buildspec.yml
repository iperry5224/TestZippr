version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.12
  pre_build:
    commands:
      - echo "Packaging SAE GRC Tools for deployment..."
  build:
    commands:
      - echo "Build started on $(date)"
      - echo "Creating deployment artifact..."

artifacts:
  files:
    - '**/*'
  discard-paths: no

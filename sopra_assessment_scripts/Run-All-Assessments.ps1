# SOPRA Assessment - Run All
# Execute on target machine with appropriate permissions (Domain Admin for AD, etc.)
# Output: sopra_assessment_results.csv - import into SOPRA

$ScriptDir = $PSScriptRoot
if (Test-Path (Join-Path $ScriptDir 'sopra_assessment_results.csv')) { Remove-Item (Join-Path $ScriptDir 'sopra_assessment_results.csv') }
'category,control_id,control_name,status,severity,evidence,notes,assessed_by,assessment_date' | Out-File (Join-Path $ScriptDir 'sopra_assessment_results.csv') -Encoding UTF8

& (Join-Path $ScriptDir 'AD_001_assessment.ps1')
& (Join-Path $ScriptDir 'AD_002_assessment.ps1')
& (Join-Path $ScriptDir 'AD_003_assessment.ps1')
& (Join-Path $ScriptDir 'AD_004_assessment.ps1')
& (Join-Path $ScriptDir 'AD_005_assessment.ps1')
& (Join-Path $ScriptDir 'AD_006_assessment.ps1')
& (Join-Path $ScriptDir 'AD_007_assessment.ps1')
& (Join-Path $ScriptDir 'AD_008_assessment.ps1')
& (Join-Path $ScriptDir 'AD_009_assessment.ps1')
& (Join-Path $ScriptDir 'AD_010_assessment.ps1')
& (Join-Path $ScriptDir 'APP_001_assessment.ps1')
& (Join-Path $ScriptDir 'APP_002_assessment.ps1')
& (Join-Path $ScriptDir 'APP_003_assessment.ps1')
& (Join-Path $ScriptDir 'APP_004_assessment.ps1')
& (Join-Path $ScriptDir 'APP_005_assessment.ps1')
& (Join-Path $ScriptDir 'APP_006_assessment.ps1')
& (Join-Path $ScriptDir 'APP_007_assessment.ps1')
& (Join-Path $ScriptDir 'APP_008_assessment.ps1')
& (Join-Path $ScriptDir 'APP_009_assessment.ps1')
& (Join-Path $ScriptDir 'APP_010_assessment.ps1')
& (Join-Path $ScriptDir 'CFG_001_assessment.ps1')
& (Join-Path $ScriptDir 'CFG_002_assessment.ps1')
& (Join-Path $ScriptDir 'CFG_003_assessment.ps1')
& (Join-Path $ScriptDir 'CFG_004_assessment.ps1')
& (Join-Path $ScriptDir 'CFG_005_assessment.ps1')
& (Join-Path $ScriptDir 'CFG_006_assessment.ps1')
& (Join-Path $ScriptDir 'CFG_007_assessment.ps1')
& (Join-Path $ScriptDir 'CFG_008_assessment.ps1')
& (Join-Path $ScriptDir 'CFG_009_assessment.ps1')
& (Join-Path $ScriptDir 'CFG_010_assessment.ps1')
& (Join-Path $ScriptDir 'CP_001_assessment.ps1')
& (Join-Path $ScriptDir 'CP_002_assessment.ps1')
& (Join-Path $ScriptDir 'CP_003_assessment.ps1')
& (Join-Path $ScriptDir 'CP_004_assessment.ps1')
& (Join-Path $ScriptDir 'CP_005_assessment.ps1')
& (Join-Path $ScriptDir 'CP_006_assessment.ps1')
& (Join-Path $ScriptDir 'CP_007_assessment.ps1')
& (Join-Path $ScriptDir 'CP_008_assessment.ps1')
& (Join-Path $ScriptDir 'CP_009_assessment.ps1')
& (Join-Path $ScriptDir 'CP_010_assessment.ps1')
& (Join-Path $ScriptDir 'CRY_001_assessment.ps1')
& (Join-Path $ScriptDir 'CRY_002_assessment.ps1')
& (Join-Path $ScriptDir 'CRY_003_assessment.ps1')
& (Join-Path $ScriptDir 'CRY_004_assessment.ps1')
& (Join-Path $ScriptDir 'CRY_005_assessment.ps1')
& (Join-Path $ScriptDir 'CRY_006_assessment.ps1')
& (Join-Path $ScriptDir 'CRY_007_assessment.ps1')
& (Join-Path $ScriptDir 'CRY_008_assessment.ps1')
& (Join-Path $ScriptDir 'CRY_009_assessment.ps1')
& (Join-Path $ScriptDir 'CRY_010_assessment.ps1')
& (Join-Path $ScriptDir 'DAT_001_assessment.ps1')
& (Join-Path $ScriptDir 'DAT_002_assessment.ps1')
& (Join-Path $ScriptDir 'DAT_003_assessment.ps1')
& (Join-Path $ScriptDir 'DAT_004_assessment.ps1')
& (Join-Path $ScriptDir 'DAT_005_assessment.ps1')
& (Join-Path $ScriptDir 'DAT_006_assessment.ps1')
& (Join-Path $ScriptDir 'DAT_007_assessment.ps1')
& (Join-Path $ScriptDir 'DAT_008_assessment.ps1')
& (Join-Path $ScriptDir 'DAT_009_assessment.ps1')
& (Join-Path $ScriptDir 'DAT_010_assessment.ps1')
& (Join-Path $ScriptDir 'ECS_001_assessment.ps1')
& (Join-Path $ScriptDir 'ECS_002_assessment.ps1')
& (Join-Path $ScriptDir 'ECS_003_assessment.ps1')
& (Join-Path $ScriptDir 'ECS_004_assessment.ps1')
& (Join-Path $ScriptDir 'ECS_005_assessment.ps1')
& (Join-Path $ScriptDir 'ECS_006_assessment.ps1')
& (Join-Path $ScriptDir 'ECS_007_assessment.ps1')
& (Join-Path $ScriptDir 'ECS_008_assessment.ps1')
& (Join-Path $ScriptDir 'ECS_009_assessment.ps1')
& (Join-Path $ScriptDir 'ECS_010_assessment.ps1')
& (Join-Path $ScriptDir 'END_001_assessment.ps1')
& (Join-Path $ScriptDir 'END_002_assessment.ps1')
& (Join-Path $ScriptDir 'END_003_assessment.ps1')
& (Join-Path $ScriptDir 'END_004_assessment.ps1')
& (Join-Path $ScriptDir 'END_005_assessment.ps1')
& (Join-Path $ScriptDir 'END_006_assessment.ps1')
& (Join-Path $ScriptDir 'END_007_assessment.ps1')
& (Join-Path $ScriptDir 'END_008_assessment.ps1')
& (Join-Path $ScriptDir 'END_009_assessment.ps1')
& (Join-Path $ScriptDir 'END_010_assessment.ps1')
& (Join-Path $ScriptDir 'GOV_001_assessment.ps1')
& (Join-Path $ScriptDir 'GOV_002_assessment.ps1')
& (Join-Path $ScriptDir 'GOV_003_assessment.ps1')
& (Join-Path $ScriptDir 'GOV_004_assessment.ps1')
& (Join-Path $ScriptDir 'GOV_005_assessment.ps1')
& (Join-Path $ScriptDir 'GOV_006_assessment.ps1')
& (Join-Path $ScriptDir 'GOV_007_assessment.ps1')
& (Join-Path $ScriptDir 'GOV_008_assessment.ps1')
& (Join-Path $ScriptDir 'GOV_009_assessment.ps1')
& (Join-Path $ScriptDir 'GOV_010_assessment.ps1')
& (Join-Path $ScriptDir 'IAM_001_assessment.ps1')
& (Join-Path $ScriptDir 'IAM_002_assessment.ps1')
& (Join-Path $ScriptDir 'IAM_003_assessment.ps1')
& (Join-Path $ScriptDir 'IAM_004_assessment.ps1')
& (Join-Path $ScriptDir 'IAM_005_assessment.ps1')
& (Join-Path $ScriptDir 'IAM_006_assessment.ps1')
& (Join-Path $ScriptDir 'IAM_007_assessment.ps1')
& (Join-Path $ScriptDir 'IAM_008_assessment.ps1')
& (Join-Path $ScriptDir 'IAM_009_assessment.ps1')
& (Join-Path $ScriptDir 'IAM_010_assessment.ps1')
& (Join-Path $ScriptDir 'IR_001_assessment.ps1')
& (Join-Path $ScriptDir 'IR_002_assessment.ps1')
& (Join-Path $ScriptDir 'IR_003_assessment.ps1')
& (Join-Path $ScriptDir 'IR_004_assessment.ps1')
& (Join-Path $ScriptDir 'IR_005_assessment.ps1')
& (Join-Path $ScriptDir 'IR_006_assessment.ps1')
& (Join-Path $ScriptDir 'IR_007_assessment.ps1')
& (Join-Path $ScriptDir 'IR_008_assessment.ps1')
& (Join-Path $ScriptDir 'IR_009_assessment.ps1')
& (Join-Path $ScriptDir 'IR_010_assessment.ps1')
& (Join-Path $ScriptDir 'MON_001_assessment.ps1')
& (Join-Path $ScriptDir 'MON_002_assessment.ps1')
& (Join-Path $ScriptDir 'MON_003_assessment.ps1')
& (Join-Path $ScriptDir 'MON_004_assessment.ps1')
& (Join-Path $ScriptDir 'MON_005_assessment.ps1')
& (Join-Path $ScriptDir 'MON_006_assessment.ps1')
& (Join-Path $ScriptDir 'MON_007_assessment.ps1')
& (Join-Path $ScriptDir 'MON_008_assessment.ps1')
& (Join-Path $ScriptDir 'MON_009_assessment.ps1')
& (Join-Path $ScriptDir 'MON_010_assessment.ps1')
& (Join-Path $ScriptDir 'NET_001_assessment.ps1')
& (Join-Path $ScriptDir 'NET_002_assessment.ps1')
& (Join-Path $ScriptDir 'NET_003_assessment.ps1')
& (Join-Path $ScriptDir 'NET_004_assessment.ps1')
& (Join-Path $ScriptDir 'NET_005_assessment.ps1')
& (Join-Path $ScriptDir 'NET_006_assessment.ps1')
& (Join-Path $ScriptDir 'NET_007_assessment.ps1')
& (Join-Path $ScriptDir 'NET_008_assessment.ps1')
& (Join-Path $ScriptDir 'NET_009_assessment.ps1')
& (Join-Path $ScriptDir 'NET_010_assessment.ps1')
& (Join-Path $ScriptDir 'PHY_001_assessment.ps1')
& (Join-Path $ScriptDir 'PHY_002_assessment.ps1')
& (Join-Path $ScriptDir 'PHY_003_assessment.ps1')
& (Join-Path $ScriptDir 'PHY_004_assessment.ps1')
& (Join-Path $ScriptDir 'PHY_005_assessment.ps1')
& (Join-Path $ScriptDir 'PHY_006_assessment.ps1')
& (Join-Path $ScriptDir 'PHY_007_assessment.ps1')
& (Join-Path $ScriptDir 'PHY_008_assessment.ps1')
& (Join-Path $ScriptDir 'PHY_009_assessment.ps1')
& (Join-Path $ScriptDir 'PHY_010_assessment.ps1')
& (Join-Path $ScriptDir 'SAT_001_assessment.ps1')
& (Join-Path $ScriptDir 'SAT_002_assessment.ps1')
& (Join-Path $ScriptDir 'SAT_003_assessment.ps1')
& (Join-Path $ScriptDir 'SAT_004_assessment.ps1')
& (Join-Path $ScriptDir 'SAT_005_assessment.ps1')
& (Join-Path $ScriptDir 'SAT_006_assessment.ps1')
& (Join-Path $ScriptDir 'SAT_007_assessment.ps1')
& (Join-Path $ScriptDir 'SAT_008_assessment.ps1')
& (Join-Path $ScriptDir 'SAT_009_assessment.ps1')
& (Join-Path $ScriptDir 'SAT_010_assessment.ps1')
& (Join-Path $ScriptDir 'SCR_001_assessment.ps1')
& (Join-Path $ScriptDir 'SCR_002_assessment.ps1')
& (Join-Path $ScriptDir 'SCR_003_assessment.ps1')
& (Join-Path $ScriptDir 'SCR_004_assessment.ps1')
& (Join-Path $ScriptDir 'SCR_005_assessment.ps1')
& (Join-Path $ScriptDir 'SCR_006_assessment.ps1')
& (Join-Path $ScriptDir 'SCR_007_assessment.ps1')
& (Join-Path $ScriptDir 'SCR_008_assessment.ps1')
& (Join-Path $ScriptDir 'SCR_009_assessment.ps1')
& (Join-Path $ScriptDir 'SCR_010_assessment.ps1')
& (Join-Path $ScriptDir 'SRV_001_assessment.ps1')
& (Join-Path $ScriptDir 'SRV_002_assessment.ps1')
& (Join-Path $ScriptDir 'SRV_003_assessment.ps1')
& (Join-Path $ScriptDir 'SRV_004_assessment.ps1')
& (Join-Path $ScriptDir 'SRV_005_assessment.ps1')
& (Join-Path $ScriptDir 'SRV_006_assessment.ps1')
& (Join-Path $ScriptDir 'SRV_007_assessment.ps1')
& (Join-Path $ScriptDir 'SRV_008_assessment.ps1')
& (Join-Path $ScriptDir 'SRV_009_assessment.ps1')
& (Join-Path $ScriptDir 'SRV_010_assessment.ps1')
& (Join-Path $ScriptDir 'VCS_001_assessment.ps1')
& (Join-Path $ScriptDir 'VCS_002_assessment.ps1')
& (Join-Path $ScriptDir 'VCS_003_assessment.ps1')
& (Join-Path $ScriptDir 'VCS_004_assessment.ps1')
& (Join-Path $ScriptDir 'VCS_005_assessment.ps1')
& (Join-Path $ScriptDir 'VCS_006_assessment.ps1')
& (Join-Path $ScriptDir 'VCS_007_assessment.ps1')
& (Join-Path $ScriptDir 'VCS_008_assessment.ps1')
& (Join-Path $ScriptDir 'VCS_009_assessment.ps1')
& (Join-Path $ScriptDir 'VCS_010_assessment.ps1')
& (Join-Path $ScriptDir 'VM_001_assessment.ps1')
& (Join-Path $ScriptDir 'VM_002_assessment.ps1')
& (Join-Path $ScriptDir 'VM_003_assessment.ps1')
& (Join-Path $ScriptDir 'VM_004_assessment.ps1')
& (Join-Path $ScriptDir 'VM_005_assessment.ps1')
& (Join-Path $ScriptDir 'VM_006_assessment.ps1')
& (Join-Path $ScriptDir 'VM_007_assessment.ps1')
& (Join-Path $ScriptDir 'VM_008_assessment.ps1')
& (Join-Path $ScriptDir 'VM_009_assessment.ps1')
& (Join-Path $ScriptDir 'VM_010_assessment.ps1')
& (Join-Path $ScriptDir 'WMS_001_assessment.ps1')
& (Join-Path $ScriptDir 'WMS_002_assessment.ps1')
& (Join-Path $ScriptDir 'WMS_003_assessment.ps1')
& (Join-Path $ScriptDir 'WMS_004_assessment.ps1')
& (Join-Path $ScriptDir 'WMS_005_assessment.ps1')
& (Join-Path $ScriptDir 'WMS_006_assessment.ps1')
& (Join-Path $ScriptDir 'WMS_007_assessment.ps1')
& (Join-Path $ScriptDir 'WMS_008_assessment.ps1')
& (Join-Path $ScriptDir 'WMS_009_assessment.ps1')
& (Join-Path $ScriptDir 'WMS_010_assessment.ps1')

Write-Host ''
Write-Host 'Done. Import sopra_assessment_results.csv into SOPRA.'